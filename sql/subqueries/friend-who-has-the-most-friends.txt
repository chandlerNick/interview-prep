# 602. Friend Request II: Who Has the Most Friends

# Write your MySQL query statement below
SELECT id, COUNT(id) num
FROM (SELECT requester_id id FROM RequestAccepted
      UNION ALL
      SELECT accepter_id id FROM RequestAccepted
     ) t  # need to count number of rows with each id.
 GROUP BY id
 ORDER BY num DESC
 LIMIT 1

