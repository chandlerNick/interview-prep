# 1204. Last Person to Fit in the Bus

# Write your MySQL query statement below
SELECT person_name
FROM (SELECT SUM(weight) OVER (ORDER BY turn) AS total_weight, person_name
      FROM Queue) t
WHERE total_weight <= 1000
ORDER BY total_weight DESC LIMIT 1

