# 178. Rank Scores

import pandas as pd

def order_scores(scores: pd.DataFrame) -> pd.DataFrame:
    scores = scores.drop(columns=["id"]).sort_values(by="score", ascending=False)
    scores["rank"] = scores.rank(method="dense", ascending=False)
    return scores


# Rank function.

