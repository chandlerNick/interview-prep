# 2889. Reshape Data: Pivot

import pandas as pd

def pivotTable(weather: pd.DataFrame) -> pd.DataFrame:
    return weather.pivot(index="month", columns="city", values="temperature")

# Notes

pivot method reshapes dataframes

index -> rows
columns -> columns
values -> value in cells
