# 452. Minmum Number of Arrows to Burst Balloons

from typing import List

class Solution:
    def findMinArrowShots(self, points: List[List[int]]) -> int:
        if not points:
            return 0
            
        # Sort strictly by the end coordinate
        points.sort(key=lambda x: x[1])
        
        arrows = 1
        curr_end = points[0][1]
        
        for start, end in points:
            # If the current balloon starts after the last arrow position,
            # we need a new arrow.
            if start > curr_end:
                arrows += 1
                curr_end = end
                
        return arrows

RT -> O(nlgn)
Space -> O(n) (from sort)
